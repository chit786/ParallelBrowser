os: osx
osx_image: xcode8.3
language: java
jdk:
  - oraclejdk8
addons:
    firefox: "53.0.3"
    chrome: stable
before_install:
      - "export DISPLAY=:99.0"
      - "sh -e /etc/init.d/xvfb start"
      - sleep 3 # give xvfb some time to start
script: mvn clean test